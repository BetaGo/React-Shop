{
  "name": "lookup-server",
  "version": "0.0.1",
  "private": true,
  "dependencies": {
    "koa": "^2.3.0",
    "koa-router": "^7.2.1",
    "redux-promise": "^0.5.3"
  },
  "scripts": {
    "start": "concurrently \"npm run server\" \"npm run client\"",
    "client": "node start-client.js",
    "server": "node server.js"
  },
  "devDependencies": {
    "concurrently": "^3.1.0"
  }
}
